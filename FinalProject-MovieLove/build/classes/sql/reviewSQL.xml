<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="review">
	<insert id="insertReview" parameterType="ReviewVO">
		insert into review(review_title, review_contents, review_rating, review_writer, review_date, review_movieid) 
		values(#{review_title}, #{review_contents}, #{review_rating}, #{review_writer}, #{review_date}, ${review_movieid})
	</insert>
	
	<select id="getMovieId" parameterType="String" resultType="String">
		select MOVIE_ID from MOVIE where MOVIE_TITLE=#{review_title}
	</select>
	
	<select id="reviewInfo" resultType="ReviewVO">
		select * from REVIEW
	</select>
	
	<select id="getReviewInfo" parameterType="String" resultType="ReviewVO">
		select * from review where review_movieid=#{value};
	</select>
</mapper>